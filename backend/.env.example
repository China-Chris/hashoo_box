# Chain
RPC_URL=https://testnet.hsk.xyz
CHAIN_ID=133
OPERATOR_PRIVATE_KEY=0x...
BLIND_BOX_ADDRESS=0x...

# Optional Vault
# VAULT_ADDRESS=0x...

# Real ZK (commit_open circuit)
# MOCK_PROOF=0
# CIRCUIT_WASM_PATH=.../commit_open.wasm
# CIRCUIT_ZKEY_PATH=.../commit_open_final.zkey

# Postgres (docker-compose up -d postgres)
# DATABASE_URL=postgresql://blindbox:blindbox@localhost:5432/blindbox

# 3C: after DB insert, operator calls registerBox on-chain
# REGISTER_ON_CHAIN=1
