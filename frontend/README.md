# Hashoo Box — Frontend (Next.js)

- **Dev:** `npm install && cp .env.example .env.local && npm run dev`
- **Build:** `npm run build`
- **Vercel:** set project **Root Directory** to `frontend`

See repository root [README.md](../README.md) for full stack setup.
