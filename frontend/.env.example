# Get a free project ID at https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=291b7b45c8ed1121cac3c993d05dee55

# Backend base URL for Mystery Box list + open flow (optional; unset = local demo data)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Wagmi 默认链为 HashKey Testnet 133（与后端 CHAIN_ID 一致）；无需单独配置
